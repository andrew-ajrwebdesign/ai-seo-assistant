# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
*.log
debug.log

# Dependencies
node_modules/
# vendor/ is intentionally committed so the plugin installs and runs directly
# from a GitHub download (the bootstrap requires vendor/autoload.php and target
# sites do not run Composer). Regenerate with: composer install --no-dev -o

# Build/package output
dist/
build/
*.zip
*.tar.gz

# Environment / secrets
.env
.env.*
*.key
*.pem

# WordPress noise
wp-cli.local.yml
.claude/
